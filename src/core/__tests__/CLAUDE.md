- Never include `{ kind: 'stdin', ... }` params in test schemas passed to `parseArgv` — `parseArgv` reads stdin to EOF for any `stdin` param, and `node --test` pipes stdin with no EOF signal, so the test hangs forever with no output or error. Test the non-stdin flags in isolation instead (see `job.test.ts` `agent new` suite for the pattern).
